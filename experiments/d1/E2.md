# E2：VisDrone 数据、官方评测与特征缓存

## 1. 范围和当前状态

2026-09-09：**E2 工程验收完成。** 数据、完整 train/val 缓存、10 类真实训练、严格恢复、
三轮全集短基准、checkpoint 重载及实际预测的官方评分均通过。
验收代码为 `f328a10907f57c66c2efb92d7b506be7cc836b8d`；
完整判定与文件摘要见 [E2 最终验收](manifests/e2-acceptance.json)。
本阶段不启动 E3 aux 扫描、E4 正式训练，也不确定最终 Scratch 架构。

| 项目 | 状态 |
| --- | --- |
| 原始数据与独立训练标签、ignore 侧记录 | 已准备，保留官方划分及全部原件 |
| 官方评测版本、预测导出、MATLAB 一致性 | 工具对齐通过；8 图与完整 548 图实际模型预测评分完成 |
| 六卡抽取、原 batch 对齐、非破坏 NPY 转换 | 完整 7,019 图已通过，保留 safetensors 原件 |
| 10 类模型、短闭环、严格恢复及训练 ETA | 已通过；三轮短基准约 17.95 秒/轮，详见第 8 节 |

## 2. 数据结果

| split | 图片 | 原始标注行 | 有效训练目标 | ignore 目标 | 退化框排除 |
| --- | ---: | ---: | ---: | ---: | ---: |
| train | 6,471 | 353,550 | 343,204 | 10,345 | 1 |
| val | 548 | 40,169 | 38,759 | 1,410 | 0 |
| test-dev | 1,610 | 77,547 | 75,102 | 2,445 | 0 |

有效类别 1..10 映射到 0..9，只有 score=1 的合法非退化框进入 YOLO 标签。
遮挡程度不作为额外过滤条件。类别 0/11、score=0、原始坐标及所有原始行均保留在 sidecar。
训练尚未实现 ignore 区域的背景梯度屏蔽，后续配对实验须披露这一限制。
test-dev 的标签准备不意味着允许用于筛选；E2 不抽取它的特征，也不评测它。

官方原件包含同 split 下三对字节相同但 ID 不同的图片：train 两对、val 一对。
保留这些原始样本并登记在 manifest，不去重、不人为补图；未发现跨 split 的 ID 或图片字节重复。
train 的一个退化框不写入训练标签，但在原件及 sidecar 中保留。

准备目录采用独立 `images/visdrone-{split}`、`labels/visdrone-{split}`、`sidecars/visdrone-{split}`。
图像通过同一 NVMe 文件系统的硬链接复用字节，**任何后续工具不得原地修改图片**；原始标注不建立可写共享副本。
完整逐图图片/标注/标签/sidecar SHA256、宽高及对应关系位于外部 `samples.jsonl`。
Git 仅保存[数据汇总](manifests/e2-visdrone-data.json)；运行时 `dataset.yaml` 含参数注入的实际路径，不提交服务器路径。

| 文件 | SHA256 |
| --- | --- |
| train 相对路径列表 | `08690be5f316240d5d31a05e7589f11dcb2bd0fa1fd1256fc0b695342e7b25a4` |
| val 相对路径列表 | `e7a836107d87985f4eaebe1af2ceaa27b52542d03fde7965561a197c283eea55` |
| test-dev 相对路径列表 | `3799abac15141e4c97bd3517488746bfc1563e1b62f864f271b7786dd8879157` |
| 完整 samples.jsonl | `b0927b2ef71d0f9a611d16b5ecb59db400b1316d787c384f3bfda392b3dd56ce` |

## 3. 官方评测协议

官方工具：[VisDrone2018-DET-toolkit](https://github.com/VisDrone/VisDrone2018-DET-toolkit/tree/005445782213e20cb91bc50a597db3dd949e749a)，
固定 commit `005445782213e20cb91bc50a597db3dd949e749a`；每个文件的 SHA256 见
[工具 manifest](manifests/visdrone-toolkit.json)。

导出原图 xywh、类别 1..10 的逐图 TXT。conf=0.001，按分数稳定降序后每图最多 500 个预测。
空预测写空 TXT，导出器不追加 NMS；模型端的端到端解码或 NMS 需另行登记，不混用后处理。
官方 `dropObjectsInIgr`、`evalRes`、`calcAccuracy`、`VOCap` 原样调用；保留其 ignore、匹配、截断及汇总行为。
AP 使用 IoU 0.50:0.05:0.95，AR 使用 maxDets 1/10/100/500。百分制原值保留，同时除以 100 输出统一比例值。
这是锁定工具的结果，不是普通 COCO AP，不与 COCO 数值混为一套指标。

封装仅增加文件与哈希检查、JSON 输出及空 TXT 的 `zeros(0,8)` 输入处理。
本机 MATLAB R2026a Update 5 可用，但缺少 Image Processing Toolbox 的 `mean2`；
提供 `mean(value(:))` 的明确兼容实现，仅在原函数不存在时启用，不修改官方匹配/AP 源文件。
MATLAB 是本方案选定的官方评测实现，不是 D1 课题强制指定的语言，也不参与训练循环。
后续可采用经过相同边界样例及真实预测数值对齐的 Python 实现；不能直接把普通 YOLO/COCO AP 改称官方 VisDrone AP。

验收：固定排序前 8 张真实 val 图片，使用 GT 派生的确定性预测，
与官方 `saveAnnoRes -> calcAccuracy` 的七项指标逐项比较，最大绝对差 **0**（门槛 1e-6）。
另通过精确 IoU 边界、重复检测、score=0 匹配、ignore 区域及十类空预测测试。
**这些是评测工具的测试夹具，不是训练模型的检测成绩。** 本阶段不引入 Python AP 替代实现。
实际测试摘要见 [MATLAB 验收](manifests/e2-visdrone-matlab-tests.json)。

## 4. 缓存协议与恢复

- DINOv3 ViT-S/16，block4/8/12，640 LetterBox 与既有 WP0 归一化；每图三组 FP16 `[384,40,40]`。
- train+val 共 7,019 图、21,057 个 tensor，原始特征约 24.10 GiB；保留 safetensors 和 NPY 时约 48.2 GiB 加元数据。
- 六进程各使用一张 GPU，batch16；排序后 `index % 6 == rank` 分区，每个 rank 的首尾批次被记录。
- 续跑不重新压紧剩余样本的 batch：部分批次缺失时重算完整原批次，只补未提交样本，保持 FP16 上下文。
- 每个 rank 有独立目录/锁/日志/收据。正式索引只在全部 rank 身份、数量、哈希通过后发布。
- 原始重复图片保留独立样本 ID；同一 rank 重复内容在分片边界隔离，避免共享 tensor key 覆盖不同批次结果。
- 对每个 rank 的首批与尾批按原上下文在线抽查，报告每层最大/平均绝对误差，门槛 rtol=atol=1e-3。
- 每图 NPY 形状为 `[3,384,40,40]`，转换前后逐层 SHA256 相同，独立读回验证；**不删除 safetensors 源件**。
- `.part` 残留、缺失 rank、错误身份和损坏文件拒绝发布；不得为恢复而删除已经校验通过的正式文件。
- 原有 `/root` 限定且删除源分片的迁移命令不变；新入口仅负责 VisDrone train/val 的非破坏转换。
- NPY 读取器增加独立 `visdrone-train/visdrone-val` 命名空间，不放宽 COCO 12 位 ID 检查。
- 数据、权重、缓存位于同一显式指定的本地文件系统，拒绝 NFS/CIFS；200 GiB 可用空间是启动门槛。

## 5. 复现入口

### 5.1 数据与缓存

从仓库根目录运行，先激活已有 D1 环境。以下路径均由使用者注入：

```bash
export NVME_ROOT=/path/to/local-nvme/d1-tmp
export E2_WORK=/path/to/external/e2-work
export D1_WEIGHTS="$NVME_ROOT/weights/dinov3-vits16-pretrain-lvd1689m"

python -m scripts.d1.prepare_visdrone \
  --source "$NVME_ROOT/datasets/VisDrone" \
  --output "$NVME_ROOT/datasets/visdrone-d1-v1"
python -m scripts.d1.prepare_visdrone \
  --source "$NVME_ROOT/datasets/VisDrone" \
  --output "$NVME_ROOT/datasets/visdrone-d1-v1" --verify-only

# 在干净提交上执行；预计超过 3 分钟应由 nohup/作业系统托管。
python -m scripts.d1.cache_visdrone all --repo "$PWD" \
  --data "$NVME_ROOT/datasets/visdrone-d1-v1" --weights "$D1_WEIGHTS" \
  --output "$NVME_ROOT/visdrone-d1-e2-v1" --devices 0,1,2,3,4,5 --batch 16

python -m scripts.d1.evaluate_visdrone fixture \
  --source "$NVME_ROOT/datasets/VisDrone/VisDrone2019-DET-val" \
  --output "$E2_WORK/evaluator-fixture"
```

正式预测导出入口（JSON 为字符串 `image_id`、0..9 `category_id`、原图 `bbox=[x,y,w,h]` 和 score）：

```bash
python -m scripts.d1.evaluate_visdrone export \
  --predictions "$E2_WORK/predictions.json" \
  --image-list "$NVME_ROOT/datasets/visdrone-d1-v1/val.txt" \
  --output "$E2_WORK/predictions-txt"
```

在有许可的 MATLAB 上添加 `scripts/d1` 到路径后调用：

```matlab
report = evaluate_visdrone_official(toolkitDir, originalSplitDir, predictionDir, outputJson, toolkitManifest);
test_visdrone_official(toolkitDir, fixtureDatasetDir, fixturePredictionDir, testOutputDir, toolkitManifest);
```

所有参数显式提供；`toolkitManifest` 指向仓库 `experiments/d1/manifests/visdrone-toolkit.json`。
大缓存、官方工具副本、真实图集与完整日志不进入 Git。

### 5.2 有界训练验收

配置见 [e2-visdrone.yaml](../../ultralytics/cfg/experiments/d1/p1p2/e2-visdrone.yaml)，
入口见 [run_e2.py](../../scripts/d1/run_e2.py)。复用已验证的 D1 Trainer 和精确恢复策略，
保留 E1 原有 100 轮/global batch384 默认值，E2 子类单独声明 300 轮调度/global batch96。
只开放下面三种运行窗口，不提供自动进入正式训练或扫描的选项。

| 步骤 | train / val | 实际执行 | 验收内容 |
| --- | --- | --- | --- |
| smoke | 排序前 96 / 8 图 | 第 1 轮，1 次全局更新 | 10 类真实缓存 loss/backward，AMP、aux 梯度、保存 checkpoint |
| smoke-resume | 相同 96 / 8 图 | 严格续跑至第 2 轮，共 2 次更新 | 恢复 model/EMA/optimizer/scaler/scheduler/criterion、6 rank RNG 和 loader generator |
| benchmark | 完整 6,471 / 548 图 | 前 3 轮，每轮 68 次更新 | 全集读取、验证、保存、显存和各 rank 数据等待计时 |

固定 ViT-S 缓存、640 输入、10 类、原九分支 Adapter、weighted_sum 等权融合、端到端 Detect；
aux 用默认 balance=0.01、z=0.001、gain=0.1、budget=3.0，以验证启用 aux 的计算路径，
不代表这组系数已经胜过关闭 aux。seed0、确定性、6 卡、每卡 batch16、global batch=nbs=96，
每 rank workers4、prefetch1、AMP init scale16/growth interval1000000，无数据增强。
AdamW lr0=0.001/lrf=0.01、momentum=0.9、weight decay=0.0005、warmup3、cos_lr=True。
**300 轮仅是完整学习率调度长度，短基准在第 3 轮末退出，不训练满 300 轮。**
DDP 为对齐各 rank，会将 6,471 张样本补齐到 6,474 次曝光，最后一批每卡 7 张；不是改变原始 split。

独立评测严格加载保存的 EMA checkpoint，要求无 Teacher 参数、所有状态及指标有限，
smoke 覆盖 8 张、benchmark 覆盖 548 张 val。模型端与导出端均固定 top500/conf0.001，
保留原图坐标和原类别映射；裁剪后的零面积预测不导出，数量单独登记。
内部 Validator AP 仅用于工程监控；本次训练模型的官方 VisDrone AP 已按第 3 节完成独立评分。

```bash
# 使用新的外部目录；不要复用已存在 matrix.json 的工作目录重复执行 all。
python -m scripts.d1.run_e2 all \
  --workspace "$E2_WORK/engineering" \
  --data "$NVME_ROOT/datasets/visdrone-d1-v1" \
  --cache "$NVME_ROOT/visdrone-d1-e2-v1/npy"

# 仅当核对了失败步骤、身份及 checkpoint 后，显式恢复对应步骤。
python -m torch.distributed.run --standalone --nproc_per_node=6 \
  --module scripts.d1.run_e2 train --workspace "$E2_WORK/engineering" --profile smoke-resume
python -m scripts.d1.run_e2 evaluate --workspace "$E2_WORK/engineering" --profile smoke
```

运行要求干净代码提交；matrix 固定代码/配置 SHA256、数据及缓存摘要、初始化和输入列表 SHA256。
所有列表、运行 YAML、日志、checkpoint 和预测留在外部目录。训练失败即停止后续步骤，不静默跳过更新或更换配方。
完整 all 流程先在最终提交上执行包括 P5 兼容性在内的回归门禁，通过后才训练。
各阶段记录 CPU 压力/配额与 GPU 使用情况；若其他 GPU 任务明显占用显存，则停止而非争抢资源。
CPU 共享干扰必须单列，不能把受干扰短基准外推为独占资源下的最优时间。
任务超 3 分钟使用后台托管，保存 PID/日志后只确认启动一次。
`AWAITING_MATLAB/ENGINEERING_PASSED` 只表示服务器短闭环通过，不能据此标记 E2 全部完成。
本次评分及证据审计结束后，外部工作区状态更新为 `COMPLETED/E2_VERIFIED`。

## 6. 已完成的缓存验收

缓存生成绑定提交 `6878f3db9ee6f18914b41518c8c4560eb75ebd84`；完整抽取、转换及校验墙钟 **430.581 秒，约 7 分 11 秒**。
NPY train 为 23,855,522,688 bytes，val 为 2,020,217,344 bytes，总计 **24.10 GiB**；
包含 7,019 图、21,057 个 tensor。转换及独立读回已全量核对哈希。
原 batch 上下文在线对齐共 299 张图片、897 个逐层比较，最大绝对误差 **0**。
本次再次核对全部转换收据/文件数量/大小，并对每个 split 的 32 个均匀分布样本重算哈希，通过；
这次快速预检不冒称重新扫描了所有缓存字节。
详见 [完整缓存证据](manifests/e2-visdrone-cache.json)。未抽取 test-dev，未启动正式检测训练。

## 7. 回归与训练验收

新增离线测试覆盖数据转换与稳定性、预测格式、分区/尾批上下文、源件保留的转换、NPY 腐坏和最终索引的失败关闭。
在干净提交 `f328a10` 上合并 COCO NPY、WP0-WP7、六卡缓存、P1/P2 合同、混合损失及 P5 兼容性回归，
实际 **300 passed、10 skipped，19.94 秒**。跳过项为本次未启用环境变量/设备条件的可选集成项，不视为已验收；
完整跳过列表、日志 SHA256 与执行版本见 [本次回归](manifests/e2-engineering-regression.json)。
收尾新增 5 项证据一致性测试后，再次回归 **305 passed、10 skipped，20.05 秒**，
记录见 [收尾校验](manifests/e2-closure-validation.json)。
数据 `--verify-only` 再次通过，列表和逐图哈希可重复生成。测试命令与日志哈希见
[初次回归记录](manifests/e2-regression.json)。
本次针对改动 Python 执行 py_compile、Ruff check/format 和 codespell；
检查工具安装在隔离工具目录，不更改训练环境依赖。未运行全仓库 pre-commit。
实际模型/恢复记录与原始证据摘要见 [工程验收结果](manifests/e2-engineering-results.json)。

| 项目 | 实际结果 |
| --- | --- |
| 下游模型 | 10 类、3,477,987 个参数；Teacher 不进入模型参数或 checkpoint |
| 小样本闭环 | 96 张 train、8 张 val；第 1 轮保存，严格恢复后执行第 2 轮，共 2 次有效更新 |
| 恢复内容 | model、EMA、optimizer、GradScaler、scheduler、criterion，以及 6 rank 的 RNG/loader generator |
| 全集短基准 | 6,471 张 train、548 张 val，3 轮、每轮 68 次更新，共 204 次 |
| 数值稳定性 | 六个 rank 无 AMP 重试；检测/aux 指标、梯度和 checkpoint 状态均有限 |
| 梯度与参数更新 | 第 3 轮九条 Adapter 分支在全部 rank 均有非零梯度；三个尺度 Router 与 residual gain 均实际更新 |
| 独立评测 | 严格加载保存的 EMA 权重，分别覆盖 8/548 张 val，原图坐标预测导出与官方评分摘要匹配 |

恢复验收证明状态被严格恢复并继续完成更新；**没有声称已与完全不中断的参照运行逐参数比较**。
真实输入的 FP32 独立梯度探针验证 active aux 正值、关闭 aux 为 0，并能反传至三个尺度的 Router。
均匀路由冷启动时 balance-only 为 0，z-only 为正，这是记录的初始状态，不代表后续 balance 永远无效。
验证日志中的 aux 项为 0 不能替代训练态检查；上述结论来自独立梯度探针和训练记录。

## 8. 短基准和官方评分

### 8.1 速度与资源

| 轮次 | train + 内部 val + 保存（秒） | 各 rank 最大累计取 batch 等待（秒） | 各 rank 最大 allocated 显存（bytes） |
| --- | ---: | ---: | ---: |
| 1 | 19.5082 | 0.02462 | 4,976,927,232 |
| 2 | 18.0153 | 0.02314 | 5,009,873,920 |
| 3 | 17.8890 | 0.02513 | 4,902,936,576 |

第 2/3 轮中位数 **17.9522 秒/轮**；最大 allocated 为 **4.67 GiB**，最大 reserved 为 **8.20 GiB**。
三轮训练进程包含启动/收尾实际耗时 **75.2391 秒**。
按该中位时间外推，60 轮约 **17.95 分钟**，300 轮约 **89.76 分钟（1.496 小时）**，六卡对应约 **8.976 GPU-hours**。
CPU 配额为 32 核；运行期间 CPU 配额限流次数和限流时间增量均为 0，CPU 压力 avg10 从 0.12 降为 0.00。
这是共享节点当时的资源状态，不是独占资源保证。

这里的等待只统计主机取得 batch 的累计等待，不包含 H2D 和计算，也不是冷盘读取带宽测试。
24.10 GiB 特征在 NVMe 上且可能命中系统页缓存；不能据此推断更大的 COCO 缓存同样没有等待。
ETA 只取两个早期 warmup epoch，未计启动、Teacher 抽取、跨机传输及独立官方评测；
后续实际检测数量和评测耗时可能变化。这不是正式实验完整耗时，更不是成本下降 50% 的结论。
完整精确值见 [短基准证据](manifests/e2-benchmark.json)。

### 8.2 实际模型评分

正式官方口径使用本次锁定 MATLAB toolkit；下表为完整 548 张 val、**第 3 轮 EMA checkpoint**，全部采用百分制：

| 指标 | 官方 VisDrone（%） |
| --- | ---: |
| AP（IoU 0.50:0.05:0.95） | 3.0618 |
| AP50 | 8.6529 |
| AP75 | 1.5506 |
| AR@1 | 0.3362 |
| AR@10 | 2.0000 |
| AR@100 | 6.5177 |
| AR@500 | 12.1654 |

同次独立评测的内部监控 mAP50-95 为 2.6650%、mAP50 为 7.5580%；两套评测的 ignore、匹配和汇总规则不同，不能混用。
8 图小样本仅进行了 2 次更新，其官方七项指标均为 0；该测试不设精度门槛，只验收训练、恢复和导出评分流程。
548 图官方评分实际耗时 **22.0004 秒**，8 图评分 **4.0987 秒**，均不涉及重训。
预测裁剪后被排除的零面积框分别为 32 和 960 个；导出后仍保留每张图的 TXT，包括空预测。

| 产物 | SHA256 |
| --- | --- |
| 第 3 轮 checkpoint | `27d1f8a1d0535b61e5849a3d9a039e677e90da021565be843d2a3d224d7235e1` |
| 548 图预测导出 manifest | `e07ba16dbea24ba9d167b4c96d83524680a6d56013461c7f94f4f18a1e488c52` |
| 548 图原始官方评分报告 | `7ad394071955edde2b0909afbc245bfcd4c6555019901aa9e3c0c244744b1d27` |

脱敏的官方评分、版本、原始结果摘要与 checkpoint 绑定见 [官方评测证据](manifests/e2-official-evaluation.json)。
原始 MATLAB 报告和日志留在本机，服务器保存脱敏评分摘要；checkpoint、逐图预测及完整训练日志保留在外部工作区。
原始独立评测 `report.json` 保留不改写，合并官方评分后的文件为对应目录内的 `completed-report.json`。
**以上是三轮工程短训练结果，不是完整收敛结果，不用于确定最终架构、aux 最优值或 P1/P2 达标。**

## 9. 完成边界

后台缓存输出 `status.json`：`RUNNING` 表示进行中；`FAILED` 需读错误及 rank 日志；
`COMPLETED/CACHE_VERIFIED` 只表示缓存已通过，不代表 E2 的小样本训练闭环和基准已经完成。
本次 E2 已完成上述全部验收并形成结果、代码版本及产物哈希的对应关系。
未抽取/评测 test-dev，未启动 E3/E4，也未确定最终 Scratch 基线。
下一阶段按最新 P1/P2 方案另行确认结构、候选矩阵及预算；E2 的完成不自动授权任何大规模实验。
